{
  "//": "Merge this into ~/.claude/settings.json (or a project .claude/settings.json). The Stop hook fires when Claude finishes a turn, so capture happens mid-session — no need to wait for a session to end. `handprint hook` is debounced (>=15 min) and runs the grab detached, so it never slows Claude down.",
  "hooks": {
    "Stop": [
      {
        "hooks": [{ "type": "command", "command": "handprint hook" }]
      }
    ]
  }
}
